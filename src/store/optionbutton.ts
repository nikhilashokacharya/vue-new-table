import { GetterTree, MutationTree, ActionTree } from 'vuex'
import { OptionButton } from "@/entities/OptionButton";
import { OptionButton as optionButton } from "@/models/OptionButton";

export const state: OptionButton = optionButton;
