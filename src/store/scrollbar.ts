import { GetterTree, MutationTree, ActionTree } from 'vuex'
import { ScrollBar } from "@/entities/ScrollBar";
import { ScrollBar as scrollBar } from "@/models/ScrollBar";

export const state: ScrollBar = scrollBar;
