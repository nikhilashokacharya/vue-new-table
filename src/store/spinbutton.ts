import { GetterTree, MutationTree, ActionTree } from 'vuex'
import { SpinButton } from "@/entities/SpinButton";
import { SpinButton as spinButton } from "@/models/SpinButton";

export const state: SpinButton = spinButton;
